import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.idevation.flurryapp',
  appName: 'flurryApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
